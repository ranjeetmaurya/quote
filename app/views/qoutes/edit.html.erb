<main class="container">
  <%= link_to sanitize("&larr; Back to qoute"), qoute_path(@qoute) %>

  <div class="header">
    <h1>Edit qoute</h1>
  </div>

  <%= turbo_frame_tag @qoute do %>
    <%= render "form", qoute: @qoute %>
  <% end %>  
</main>